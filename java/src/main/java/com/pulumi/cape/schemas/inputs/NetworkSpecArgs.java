// *** WARNING: this file was generated by pulumi-language-java. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

package com.pulumi.cape.schemas.inputs;

import com.pulumi.cape.schemas.inputs.CidrArgs;
import com.pulumi.cape.schemas.inputs.ReferenceArgs;
import com.pulumi.core.Output;
import com.pulumi.core.annotations.Import;
import com.pulumi.exceptions.MissingRequiredPropertyException;
import java.util.List;
import java.util.Objects;
import java.util.Optional;
import javax.annotation.Nullable;


/**
 * A Network represents a virtual network that can be used to isolate resources. Key network concepts: * Defines a range of IP addresses for compute resources (e.g. instances) * Enables network segmentation and isolation * Provides common performance configuration across the network using a SKU The `cidr` is the base CIDR block for the network. Additional CIDR blocks can be added to the network using the `additionalCidrs` field. The `additionalCidrs` can be changed after the network is created in case they are not used. The main CIDR block cannot be changed after the network is created. All cidrs must be non-overlapping. The cidrs have to be part of the RFC 1918 address space in case of IPv4 and or RFC 4193. In case the system should automatically assign IP addresses to the network cidrs only the network prefix is required. E.g. to request a /16 IPv4 CIDR block the CIDR block would be `0.0.0.0/16` and for a /56 IPv6 CIDR block the CIDR block would be `::/56`. Most CSP will not allow to use a different IPv6 prefix length than /56. Route tables associated with this network automatically include local routes for all network CIDRs (including `additionalCidrs`). These network-local routes are present by default and cannot be removed.
 * 
 */
public final class NetworkSpecArgs extends com.pulumi.resources.ResourceArgs {

    public static final NetworkSpecArgs Empty = new NetworkSpecArgs();

    @Import(name="additionalCidrs")
    private @Nullable Output<List<CidrArgs>> additionalCidrs;

    public Optional<Output<List<CidrArgs>>> additionalCidrs() {
        return Optional.ofNullable(this.additionalCidrs);
    }

    /**
     * Combined IPv4 and IPv6 CIDR block for a subnet. Depending on the network configuration, either the IPv4 or IPv6 range can be omitted. So the following combinations are possible: * IPv4 only * IPv6 only * IPv4 and IPv6 (Dual Stack)
     * 
     */
    @Import(name="cidr", required=true)
    private Output<CidrArgs> cidr;

    /**
     * @return Combined IPv4 and IPv6 CIDR block for a subnet. Depending on the network configuration, either the IPv4 or IPv6 range can be omitted. So the following combinations are possible: * IPv4 only * IPv6 only * IPv4 and IPv6 (Dual Stack)
     * 
     */
    public Output<CidrArgs> cidr() {
        return this.cidr;
    }

    /**
     * Reference to the SKU used by default for all NIC in this Network. Can be overridden by the NIC. The SKU is immutable after the network is created. To change the SKU, the network must be deleted and recreated with the new SKU reference.
     * 
     */
    @Import(name="skuRef", required=true)
    private Output<ReferenceArgs> skuRef;

    /**
     * @return Reference to the SKU used by default for all NIC in this Network. Can be overridden by the NIC. The SKU is immutable after the network is created. To change the SKU, the network must be deleted and recreated with the new SKU reference.
     * 
     */
    public Output<ReferenceArgs> skuRef() {
        return this.skuRef;
    }

    private NetworkSpecArgs() {}

    private NetworkSpecArgs(NetworkSpecArgs $) {
        this.additionalCidrs = $.additionalCidrs;
        this.cidr = $.cidr;
        this.skuRef = $.skuRef;
    }

    public static Builder builder() {
        return new Builder();
    }
    public static Builder builder(NetworkSpecArgs defaults) {
        return new Builder(defaults);
    }

    public static final class Builder {
        private NetworkSpecArgs $;

        public Builder() {
            $ = new NetworkSpecArgs();
        }

        public Builder(NetworkSpecArgs defaults) {
            $ = new NetworkSpecArgs(Objects.requireNonNull(defaults));
        }

        public Builder additionalCidrs(@Nullable Output<List<CidrArgs>> additionalCidrs) {
            $.additionalCidrs = additionalCidrs;
            return this;
        }

        public Builder additionalCidrs(List<CidrArgs> additionalCidrs) {
            return additionalCidrs(Output.of(additionalCidrs));
        }

        public Builder additionalCidrs(CidrArgs... additionalCidrs) {
            return additionalCidrs(List.of(additionalCidrs));
        }

        /**
         * @param cidr Combined IPv4 and IPv6 CIDR block for a subnet. Depending on the network configuration, either the IPv4 or IPv6 range can be omitted. So the following combinations are possible: * IPv4 only * IPv6 only * IPv4 and IPv6 (Dual Stack)
         * 
         * @return builder
         * 
         */
        public Builder cidr(Output<CidrArgs> cidr) {
            $.cidr = cidr;
            return this;
        }

        /**
         * @param cidr Combined IPv4 and IPv6 CIDR block for a subnet. Depending on the network configuration, either the IPv4 or IPv6 range can be omitted. So the following combinations are possible: * IPv4 only * IPv6 only * IPv4 and IPv6 (Dual Stack)
         * 
         * @return builder
         * 
         */
        public Builder cidr(CidrArgs cidr) {
            return cidr(Output.of(cidr));
        }

        /**
         * @param skuRef Reference to the SKU used by default for all NIC in this Network. Can be overridden by the NIC. The SKU is immutable after the network is created. To change the SKU, the network must be deleted and recreated with the new SKU reference.
         * 
         * @return builder
         * 
         */
        public Builder skuRef(Output<ReferenceArgs> skuRef) {
            $.skuRef = skuRef;
            return this;
        }

        /**
         * @param skuRef Reference to the SKU used by default for all NIC in this Network. Can be overridden by the NIC. The SKU is immutable after the network is created. To change the SKU, the network must be deleted and recreated with the new SKU reference.
         * 
         * @return builder
         * 
         */
        public Builder skuRef(ReferenceArgs skuRef) {
            return skuRef(Output.of(skuRef));
        }

        public NetworkSpecArgs build() {
            if ($.cidr == null) {
                throw new MissingRequiredPropertyException("NetworkSpecArgs", "cidr");
            }
            if ($.skuRef == null) {
                throw new MissingRequiredPropertyException("NetworkSpecArgs", "skuRef");
            }
            return $;
        }
    }

}
